{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "CodeLearn 桌面端默认权限：core 默认 + shell 执行（用于 run_native 调起子进程；实际子进程由 Rust 侧 Command 启动，无需前端 shell 插件，但保留以备扩展）",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
